Flask==2.3.2
# Pin LangChain and the Google GenAI provider to the versions you test with.
# Replace the following placeholders with the exact versions you validated:
langchain==0.0.0  # <-- replace with tested version
langchain-google-genai==0.0.0  # <-- replace with tested package/version if used
google-generativeai==0.11.0
faiss-cpu==1.7.4
gunicorn==20.1.0
